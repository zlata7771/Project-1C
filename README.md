# Project-1C
Каюкина Злата и Николаева Полина 0907-32

## open_or_create_file
C

FILE* open_or_create_file(const char* filename);
Описание:  
Открывает или создает и открывает файл файловой системы.  
Параметры:  
- filename - строка (const char*), содержащая имя файла.  

Возвращаемое значение:  
Указатель на файл (FILE*). В случае ошибки возвращает NULL.  

---

## view_file_content
C

char* view_file_content(const char* filename, FILE* fs_file);
Описание:  
Просматривает содержимое указанного файла внутри файловой системы.  
Параметры:  
- filename - строка (const char*), содержащая имя файла для просмотра.  
- fs_file - указатель на файл (FILE*), представляющий файловую систему.  

Возвращаемое значение:  
Строка (char*), содержащая содержимое файла. Необходимо освободить память после использования. В случае ошибки возвращает NULL.  

---

## delete_file
C

int delete_file(const char* filename, FILE* fs_file);
Описание:  
Удаляет файл внутри файловой системы.  
Параметры:  
- filename - строка (const char*), содержащая имя файла для удаления.  
- fs_file - указатель на файл (FILE*), представляющий файловую систему.  

Возвращаемое значение:  
Целое число (int):  
- 0 - успешное удаление.  
- -1 - ошибка (файл не найден или ошибка записи).  

---

# Файловая система - filesystem.c (Обновление)

## Документация новых функций

## create_new_file
C

int create_new_file(const char* filename, const char* content, FILE* fs_file);
Описание:  
Создает новый файл внутри файловой системы, добавляя его в конец.  
Параметры:  
- filename - строка (const char*), имя создаваемого файла  
- content - строка (const char*), содержимое файла  
- fs_file - указатель на файл (FILE*), представляющий файловую систему  

Возвращаемое значение:  
- 0 - успешное создание  
- -1 - ошибка  

---

## modify_file
C

int modify_file(const char* filename, const char* new_content, FILE* fs_file);
Описание:  
Изменяет содержимое существующего файла в файловой системе.  
Параметры:  
- filename - строка (const char*), имя изменяемого файла  
- new_content - строка (const char*), новое содержимое файла  
- fs_file - указатель на файл (FILE*), представляющий файловую систему  

Возвращаемое значение:  
- 0 - успешное изменение  
- -1 - ошибка (файл не найден)  

---
