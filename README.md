# Project-1C
Каюкина Злата и Николаева Полина 0907-32

## open_or_create_file
C

FILE* open_or_create_file(const char* filename);
Описание:  
Открывает или создает и открывает файл файловой системы.  
Параметры:  
- filename - строка (const char*), содержащая имя файла.  

Возвращаемое значение:  
Указатель на файл (FILE*). В случае ошибки возвращает NULL.  

---

## view_file_content
C

char* view_file_content(const char* filename, FILE* fs_file);
Описание:  
Просматривает содержимое указанного файла внутри файловой системы.  
Параметры:  
- filename - строка (const char*), содержащая имя файла для просмотра.  
- fs_file - указатель на файл (FILE*), представляющий файловую систему.  

Возвращаемое значение:  
Строка (char*), содержащая содержимое файла. Необходимо освободить память после использования. В случае ошибки возвращает NULL.  

---

## delete_file
C

int delete_file(const char* filename, FILE* fs_file);
Описание:  
Удаляет файл внутри файловой системы.  
Параметры:  
- filename - строка (const char*), содержащая имя файла для удаления.  
- fs_file - указатель на файл (FILE*), представляющий файловую систему.  

Возвращаемое значение:  
Целое число (int):  
- 0 - успешное удаление.  
- -1 - ошибка (файл не найден или ошибка записи).  

---

# Файловая система - filesystem.c (Обновление)

## Документация новых функций

## create_new_file
C

int create_new_file(const char* filename, const char* content, FILE* fs_file);
Описание:  
Создает новый файл внутри файловой системы, добавляя его в конец.  
Параметры:  
- filename - строка (const char*), имя создаваемого файла  
- content - строка (const char*), содержимое файла  
- fs_file - указатель на файл (FILE*), представляющий файловую систему  

Возвращаемое значение:  
- 0 - успешное создание  
- -1 - ошибка  

---

## modify_file
C

int modify_file(const char* filename, const char* new_content, FILE* fs_file);
Описание:  
Изменяет содержимое существующего файла в файловой системе.  
Параметры:  
- filename - строка (const char*), имя изменяемого файла  
- new_content - строка (const char*), новое содержимое файла  
- fs_file - указатель на файл (FILE*), представляющий файловую систему  

Возвращаемое значение:  
- 0 - успешное изменение  
- -1 - ошибка (файл не найден)  

---

# Командный интерфейс файловой системы

Синтаксис команд:
создать <имя_файла> <содержимое>
Пример:
> создать test.txt "Привет мир"
Файл 'test.txt' создан
удалить <имя_файла>
Пример:
> удалить test.txt
Файл 'test.txt' удален
изменить <имя_файла> <новое_содержимое>
Пример:
> изменить test.txt "Новое содержимое"
Файл 'test.txt' изменен
просмотреть <имя_файла>
Пример:
> просмотреть test.txt
< Новое содержимое
помощь
Выводит список всех доступных команд
выход
Завершает работу с файловой системой

## 3-6. Обновление презентации

### Слайд 3: Командный интерфейсДоступные команды:
1. создать - создание нового файла
2. удалить - удаление существующего файла
3. изменить - модификация содержимого файла
4. просмотреть - чтение содержимого файла
5. помощь - отображение справки
6. выход - завершение работы

### Слайд 4: Логотип файловой системы
![Логотип SimpleFS](logo.png)  
SimpleFS - простая и надежная файловая система

(Логотип должен быть создан и добавлен в репозиторий)

## Pull Request

1. Все изменения выполнены в ветке dev
2. Создан Pull Request из dev в main
3. В описании PR указаны все внесенные изменения:
   - Реализация CLI интерфейса
   - Обновление документации
   - Добавление логотипа
   - Обновление презентации
4. Проведено ревью кода между участниками команды
5. После одобрения PR будет выполнен мердж в main

Наша файловая система предоставляется как библиотека, состоящая из двух файлов:
- filesystem.h - заголовочный файл с объявлениями функций
- filesystem.c - реализация функций

Как подключить к проекту:

1. Добавьте оба файла в ваш проект
2. Включите заголовочный файл в местах использования:
#include "filesystem.h"
3. При компиляции укажите оба исходных файла:
gcc main.c filesystem.c -o myprogram

Доступные функции:
// Открытие/создание файловой системы
FILE* open_or_create_filesystem(const char* filename);

// Просмотр содержимого файла
char* view_file_in_filesystem(const char* fs_content, const char* filename);

// Удаление файла
int delete_file_in_filesystem(char** fs_content, const char* filename);

// Создание нового файла
int create_new_file_in_filesystem(char** fs_content, const char* filename, const char* content);

// Изменение файла
int modify_file_in_filesystem(char** fs_content, const char* filename, const char* new_content);
